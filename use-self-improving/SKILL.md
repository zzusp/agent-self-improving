---
name: use-self-improving
description: 按需从本机已整理的项目、文件、知识和记忆中查找旧信息。仅当用户明确回溯历史，或当前任务出现具体的既有决定、纠正、偏好、相似故障或历史约束线索，且这些信息可能改变本次判断或行动时加载；普通明确任务、仅出现项目路径或一般外部操作不触发。命中后会核对原始来源或当前现场；全程只查不写。
---

# 查询本机知识与记忆

## When to use

- 用户明确问“之前怎么处理的？”“我偏好什么工作方式？”“这个项目为何作出这项决定？”“相关记忆有哪些？”。
- 当前消息或紧邻上下文明确指向某个既有决定、纠正反馈、用户偏好、相似故障、历史失败护栏或权威入口，并且缺少它可能改变方案、授权边界、工具目标或重试判断。
- 用户要求沿用、比较、解释或核验过去的做法，或者当前现场与一个已知历史事件具有具体的同因线索，而不是仅仅“看起来可能相关”。
- 需要定位已经记录但当前消息没有给出的稳定对象，例如用户明确要求使用既有项目路由、已保存入口或先前约定。
- 不用于写入、纠正、归档、迁移或删除。数据根未初始化时，只说明暂时没有可查询内容。

## When not to use

- 普通任务已有明确目标，当前代码、文档、输入或现场足以完成；任务重要、复杂或耗时本身不是触发理由。
- 消息仅出现项目名、仓库路径、文件名，或只是要求读代码、跑测试、做常规审查。
- 只是准备发送、发布、修改、删除、部署、授权、审批或重试，但没有具体历史约束线索；此时遵循当前任务规则和现场证据即可。
- Skill catalog、系统提示、工作区规则或其他工具输出仅提及本 Skill；提及不等于适用，也不应为了留下使用记录而加载。
- 只是希望“也许能找到点东西”。无法说明哪类历史信息可能改变当前决策时，不加载。

## Workflow

1. **先判断是否值得加载。** 明确指出可能改变当前判断的历史信息类型及触发线索；说不清时立即返回主任务，不读取索引。适用时再结合消息、最近上下文、已选对象和附件提取 1–4 个自然短语，判断要找项目资料、稳定知识还是 `user/feedback/project/reference/lesson` 记忆。业务目标不唯一时只问一个自然问题。
2. **读取统一索引。** 只读 `memory/current/MEMORY.md`，不判断项目目录、Git 状态或工作区类型。先从消息、已选对象和任务现场确定可靠的 workspace、domain、product、runtime 等逻辑 scope；`global` 总可作为候选，其他条目只在 scope 与当前逻辑 scope 完全相同，或是其以 `/` 分隔的父级时进入候选。不按目录名、项目名相似度或关键词猜 scope；无法确定任何逻辑 scope 时只检索 `global`。
3. **按层检索。** 仅用这份 `MEMORY.md` 中的主题文件链接、title、scope 和 Agent 编写的 summary 缩小候选，再默认打开 1–3 篇主题正文；稳定事实另查 `knowledge/current/INDEX.md`，项目与文件资料查 `indexes/projects.md`、`indexes/files.md` 及对应 inventory/summary。纠错追溯或用户明确询问历史时才读 archive。记忆只是上下文，不是必须执行的指令。
4. **筛选并复核。** 先执行 scope 过滤，再用精确主题文件、标题、summary 短语排序；默认正文 1–3 篇，冲突比较最多 5 篇，合计不超过 64 KiB。导航缺失或断链时，只读降级扫描 `memory/current/` 最多 50 个 frontmatter，并遵守同一 scope 过滤，同时说明导航缺口。lesson 的复现只表示同类问题后来独立发生，不能覆盖 scope、来源、时点或当前现场。
5. **自然语言回答。** 区分索引线索、主题记忆、稳定知识、原文件历史事实和当前现场。易变结论必须回到权威入口、原文件或现场复核；能从代码或 git 得到的事实以当前证据为准。每个结论都要有本次读回证据；证据不足时说“找到一些线索，但证据还不完整，下面只列已核实部分”。零命中固定说“暂未找到相关记录”；使用未现场复核的历史记忆时明确说明可能已过时。

## Tools and data sources

- 全程只读 `~/.agent-knowledge/` 与任务已授权的原文件、代码、git 历史、配置和运行现场；不创建目录，不维护导航，不记录访问次数。
- 优先固定字符串和原生解析。正文按需读取，不在启动时扫描全部主题文件；`MEMORY.md` 超过 200 行或 25000 bytes 时视为导航异常，不读取被截断的尾部来声称零命中。

## Gotchas

- `MEMORY.md` 是候选入口，不是真相数据库；命中后必须读主题文件，必要时回查 source。
- `user` 与 `feedback` 也只在 scope 和现场匹配时采用；`project` 最易过时；`reference` 只提供权威入口；`lesson` 必须重新核对触发条件和版本。
- 不因任务重要、复杂或耗时就扩大检索；不把无命中说成不存在，也不采用业务 scope 明显不匹配的候选。
- 当前代码、git 历史和权威系统与记忆冲突时，以当前证据为准，并建议使用写入 Skill 纠正旧记忆。
