# 项目导航

此表只导航用户确认或权威入口明确登记的常用项目。扫描发现本身不是收录理由；相同 origin 的不同物理路径不得合并。

| name | path | directory_type | version_control | origin | role | key_entries | distillation_collections | last_verified |
|---|---|---|---|---|---|---|---|---|
| `<name>` | `<exact-root-path>` | `<code-project|document-collection|mixed-general>` | `<git|other|none|unknown>` | `<git-origin-or-empty>` | `<verified-role-or-unknown>` | `<semicolon-separated-paths>` | `<semicolon-separated-collection-slugs>` | `<YYYY-MM-DD>` |

`directory_type` 与版本控制分开；origin 仅在 Git 现场可核时填写。每行继续链接 `indexes/projects/<project-slug>/README.md`。
